package algorithms.heap

import collection.mutable.ArrayBuffer

/**
 * User: vasily
 * Date: 21.12.12
 */
class Heap  {
  val data: ArrayBuffer[Int] = new ArrayBuffer[Int]()

  def insert(element: Int) { insert0(element, 0);}

  def insert0(element: Int, index: Int) {
  }


}
